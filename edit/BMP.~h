//---------------------------------------------------------------------------

#ifndef BMPH
#define BMPH
#include "Imago.h"

class BMP//:public Imago
{
        AnsiString fileName;
        //TFileStream *imageFile;
        AnsiString message;
        public:
        BMP(AnsiString fileName="imagini\\default.txt");
        void createImage();
        AnsiString createText(AnsiString fileName);
        void Codificare(AnsiString);
        ~BMP();

};
//---------------------------------------------------------------------------
#endif
